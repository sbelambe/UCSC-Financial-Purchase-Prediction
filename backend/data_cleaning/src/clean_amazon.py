from __future__ import annotations

import pandas as pd


def clean_amazon(df: pd.DataFrame) -> pd.DataFrame:
    """
    Placeholder cleaner.

    Later: normalize columns, parse dates, amounts, vendors, etc.
    For now: returns df unchanged.
    """
    return df
